<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<property name="LOGS_DIR" value="C:/ratefeed/log" />
	<property name="SOURCE_NAME" value="ECB" />

	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>%d %p %C [%t] %m%n</Pattern>
		</layout>
	</appender>

	<appender name="ROLLINGFILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_DIR}/ratefeed_${SOURCE_NAME}.log</file>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>%d %p %C [%t] %m%n</Pattern>
		</encoder>

		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOGS_DIR}/archived/ratefeed_${SOURCE_NAME}-%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>10MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
	</appender>
	
	<appender name="ROLLINGFILE_FULL" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_DIR}/ratefeed_${SOURCE_NAME}_full.log</file>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>%d %p %C [%t] %m%n</Pattern>
		</encoder>

		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOGS_DIR}/archived/ratefeed_${SOURCE_NAME}_full-%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>50MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
	</appender>

	<logger name="com.pc" level="DEBUG" additivity="true">
		<appender-ref ref="ROLLINGFILE" />
		<appender-ref ref="CONSOLE" />		
	</logger>

	<root level="DEBUG">
		<appender-ref ref="ROLLINGFILE_FULL"/>
	</root>

</configuration>